{% extends 'base.html.twig' %}

{% block body %}
    <section>
        <div class="container">
            <h1 class="my-5">{{project.title}}</h1>

            <p>{{project.description}}</p>

            <p>{{project.investments|length}} intéressés</p>

        

            <a class="btn btn-primary" role="button" href="#">Investir</a>
            <a class="btn btn-primary" role="button" href="#">Simuler</a>
        </div>
    </section>
{% endblock %}